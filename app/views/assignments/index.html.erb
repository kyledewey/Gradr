<% @courses.each do |course| %>
    <h1><%= course.title %></h1>
    <div id="active-course-listing">
      <% course.assignments.each do |assignment| %>
          <h5><%= assignment.number %>: <%= link_to assignment.title, assignment_path(assignment) %></h5>
      <% end %>
    </div>
<% end %>
